Mobile Responsive NavBar
In this lab, you have to create a responsive mobile navbar which shows up after hitting a 640px width.

Challenges:
Create a main nav element
Make the nav element fixed to the bottom of the screen
Populate the nav element with the svg elements
Using media queries make sure that the nav bar is only visible for the screen size less than 640px
